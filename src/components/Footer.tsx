import React from 'react'
import { AppBar } from "@mui/material"
const Footer = () => {
  return (
    <AppBar position="absolute" color="primary" sx={{ top: 'auto', bottom: 0 }}>Footer</AppBar>
  )
}

export default Footer